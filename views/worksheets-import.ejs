<!DOCTYPE html>
<html lang="pt">
<head>
<meta charset="UTF-8" />
<title>Importar Worksheet</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-2xl shadow w-full max-w-lg">
    <h1 class="text-2xl font-bold text-indigo-600 mb-6 text-center">Importar Folha de Obra</h1>

    <% if (error) { %>
      <div class="mb-4 bg-red-50 text-red-700 border border-red-200 px-4 py-2 rounded"><%= error %></div>
    <% } %>

    <form action="/worksheets/import" method="POST" enctype="multipart/form-data" class="space-y-5">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Ficheiro GeoJSON</label>
        <input type="file" name="geojson" accept=".geojson,application/json" required
               class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:outline-none" />
      </div>
      <button type="submit" class="w-full py-3 bg-indigo-600 text-white rounded-xl shadow hover:bg-indigo-700 transition">
        Importar
      </button>
    </form>

    <div class="text-center mt-4">
      <a href="/worksheets" class="text-sm text-gray-600 hover:underline">← Voltar à lista</a>
    </div>
  </div>
</body>
</html>
